---
description: Custom rules for the agent to follow
---
# Branching Rules

- **NEVER push directly to `main`**, `master`, or `develop`.
- Always create a descriptive feature or fix branch (e.g., `feat/`, `fix/`, `chore/`).
- Only push to the tracking branch of the feature/fix you are working on.
